# Codex CLI config for CallMyAgent workers.
# Runtime credentials and model selection are passed via environment variables
# and command-line flags in /scripts/entrypoint.sh.

sandbox_mode = "workspace-write"
approval_policy = "never"
